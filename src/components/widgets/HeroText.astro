---
import type { CallToAction } from '~/types';
import Button from '~/components/ui/Button.astro';

export interface Props {
  title?: string;
  subtitle?: string;
  tagline?: string;
  content?: string;
  callToAction?: string | CallToAction;
  callToAction2?: string | CallToAction;
}

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  content = await Astro.slots.render('content'),
  callToAction = await Astro.slots.render('callToAction'),
  callToAction2 = await Astro.slots.render('callToAction2'),
} = Astro.props;
---

<section class="aw-hero not-prose">
  <div class="aw-container" style="text-align: center;">
    {tagline && (
      <p class="aw-eyebrow" style="justify-content: center;" set:html={tagline} />
    )}
    {title && (
      <h1 class="aw-display" style="margin-left: auto; margin-right: auto;" set:html={title} />
    )}
    {subtitle && (
      <p class="aw-lede" style="margin: 0 auto 2rem;" set:html={subtitle} />
    )}
    {(callToAction || callToAction2) && (
      <div class="aw-actions aw-actions-center">
        {callToAction && (
          typeof callToAction === 'string' ? (
            <Fragment set:html={callToAction} />
          ) : (
            <Button variant="primary" {...callToAction} />
          )
        )}
        {callToAction2 && (
          typeof callToAction2 === 'string' ? (
            <Fragment set:html={callToAction2} />
          ) : (
            <Button {...callToAction2} />
          )
        )}
      </div>
    )}
    {content && <Fragment set:html={content} />}
  </div>
</section>
