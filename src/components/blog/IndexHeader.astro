---
export interface Props {
  eyebrow: string;
  title: string;
  lede?: string;
}

const { eyebrow, title, lede } = Astro.props;
---

<p class="aw-eyebrow">{eyebrow}</p>
<h1 class="aw-display" style="max-width: 22ch;">{title}</h1>
{lede && <p class="aw-lede" style="max-width: 56ch;">{lede}</p>}
