---
export interface Props {
  eyebrow: string;
  title: string;
  lede?: string;
}

const { eyebrow, title, lede } = Astro.props;
---

<p class="aw-eyebrow">{eyebrow}</p>
<h1 class="aw-h2">{title}</h1>
{lede && <p class="aw-page-head-desc">{lede}</p>}
