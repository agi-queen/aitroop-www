---
import Layout from '~/layouts/PageLayout.astro';
import AnatomyFlow from '~/components/widgets/AnatomyFlow.astro';
import WorkflowLifecycle from '~/components/widgets/WorkflowLifecycle.astro';
import PopText from '~/components/ui/PopText.astro';
import GridBackground from '~/components/ui/GridBackground.astro';
import BentoGrid from '~/components/ui/BentoGrid.astro';
import type { BentoItem } from '~/components/ui/BentoGrid.astro';
import Marquee from '~/components/ui/Marquee.astro';

const metadata = {
  title: 'Aitroop - 把 AI Agent 對話固化成 App',
  ignoreTitleTemplate: true,
};

const anatomySteps = [
  {
    title: 'AppInput',
    desc: '基於 Schema 的表單。text、select、daterange、file，宣告一次，自動渲染。',
    foot: '4 種輸入類型 · 內建驗證',
  },
  {
    title: 'Stage',
    desc: '帶明確目標的 Agent 會話，連入你的技能與整合。需要時多 Stage 串聯成管道。',
    foot: '150+ 技能 · 20+ 整合',
  },
  {
    title: 'Artifact',
    desc: '可交付的產物。預覽、匯出，或直接餵給下一個 Stage。',
    foot: '7 種格式 · 預覽 + 匯出',
  },
];

const skills = [
  'GitHub', 'Gmail', 'Slack', 'Notion', 'Drive', 'Linear',
  'Stripe', 'HubSpot', 'Salesforce', 'Figma', 'Jira', 'Asana',
  'Webhook', '網頁搜尋', '程式碼執行', 'PDF', 'CSV', 'Postgres',
  'Discord', 'Twitter', 'YouTube', '日曆', 'Sheets', 'Airtable',
];

const testimonials = [
  {
    quote: '我們用 3 個 App 取代了一個 4 人運營小組的日常循環。它們依 cron 自動運行，沒人需要再去碰它們。',
    name: 'Marta R.',
    title: '營收營運主管',
    company: 'Northbound',
    featured: true,
  },
  {
    quote: '「存為 App」讓我那堆混亂的 prompt 筆記，變成了團隊真的能跑起來的東西。',
    name: 'Diego K.',
    title: '解決方案架構師',
    company: 'Plate',
  },
  {
    quote: '沙盒才是殺手鐧。真實的檔案存取、真實的工具、真實的產物——不是玩具 demo。',
    name: 'Priya S.',
    title: 'AI 總監',
    company: 'Vector Labs',
  },
];

const features: BentoItem[] = [
  {
    title: '把對話存為 App',
    desc: '一鍵沉澱剛跑通的流程，變成帶型別化輸入、可重複執行的工作流程。',
    icon: 'M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z',
    span: 'wide',
  },
  {
    title: '多 Stage 管道',
    desc: '單次 Agent 解不了的任務，就用 Stage 串聯起來。每個 Stage 擁有自己的模型、技能與 prompt。',
    icon: 'M4 6h16M4 12h16M4 18h10',
  },
  {
    title: '150+ 技能 · 20+ 整合',
    desc: 'GitHub、Gmail、Slack、Drive、Notion、網頁搜尋、程式碼執行，一鍵啟用。',
    icon: 'M3 3h7v7H3zM14 3h7v7h-7zM3 14h7v7H3zM14 14h7v7h-7z',
  },
  {
    title: '真實沙盒',
    desc: '每一次執行都跑在隔離環境中，提供檔案系統、網路和合適的工具鏈。',
    icon: 'M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z',
  },
  {
    title: '排程與 Webhook',
    desc: '依 cron 定時、手動觸發，或由 Webhook 啟動。設定一次長期託管，執行歷史可追溯。',
    icon: 'M12 6v6l4 2M12 22a10 10 0 1 1 0-20 10 10 0 0 1 0 20z',
  },
  {
    title: '團隊共享',
    desc: '每個 App 都是帶權限的物件。可以分享給同事、外部協作者，或作為內部 API 暴露。',
    icon: 'M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2M9 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8zM23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75',
  },
];

const cases = [
  {
    slug: '/zh-TW/use-cases/gtm',
    title: 'GTM',
    desc: '線索挖掘、外部觸達、客戶留存、管道洞察。',
    points: ['規模化的 ICP 研究', '個人化外聯序列', '管道與留存報告'],
  },
  {
    slug: '/zh-TW/use-cases/finance',
    title: '財務',
    desc: '月結、稽核、差異分析、報告。專為 CFO 團隊設計。',
    points: ['月結報告包自動組裝', '差異與變動分析', '稽核就緒的匯出'],
  },
];

const faqs = [
  {
    q: '把對話存成 App，要不要重寫 prompt？',
    a: '不必。在已跑通的對話裡點一下「存為 App」，Aitroop 會把當時使用的技能、整合與 prompt 都打包成可執行的工作流程。',
  },
  {
    q: 'App 儲存好之後，誰來執行？',
    a: '每一次執行都跑在隔離沙盒中，自帶檔案系統、網路與 Agent 需要的工具。可以手動觸發、依 cron 定時，或由 Webhook 啟動。',
  },
  {
    q: '步驟需要用不同的工具，能串起來嗎？',
    a: '可以。Stage 讓你把一個 Agent 接到下一個 Agent，每個 Stage 擁有獨立的模型、技能與 prompt。前一個 Stage 的產物自動成為下一個的輸入。',
  },
  {
    q: 'App 發布之後誰可以使用？',
    a: 'App 是帶權限的物件。可以分享給同事、外部協作者，或作為內部 API 暴露。執行歷史對管理員可見。',
  },
];
---

<Layout metadata={metadata}>

  <!-- HERO — single backdrop (grid only) ─────────────────────────── -->
  <section class="aw-hero hero relative isolate overflow-hidden">
    <GridBackground variant="grid" tone="brand" />
    <div class="aw-container-wide">
      <div class="hero-copy">
        <a href="/zh-TW/use-cases" class="hero-pill">
          <span class="pill-tag">NEW</span>
          <span class="pill-text">150+ 技能 · 20+ 整合</span>
          <svg class="pill-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </a>
        <h1 class="aw-display hero-display">
          強大的 Agent。<br />
          無盡的技能。<br />
          <PopText
            text="一鍵即用的 App。"
            stagger={28}
            delay={220}
            duration={420}
            burst={false}
            class="text-primary hero-pop"
          />
        </h1>
        <p class="aw-lede hero-lede">
          一鍵就把剛跑通的 Agent 流程沉澱為 App，團隊可以反覆運行。不必再敲 prompt，不必再寫膠水程式碼。
        </p>
        <div class="aw-actions hero-actions">
          <a href="https://app.aitroop.net" class="aw-btn aw-btn-primary">免費開始</a>
          <a href="/zh-TW/use-cases" class="aw-btn aw-btn-ghost">查看案例</a>
        </div>
      </div>
    </div>
  </section>

  <!-- SKILLS — single lede (replaces stats strip) + dual marquee ── -->
  <section class="skills-section">
    <div class="aw-container-wide">
      <p class="skills-lede">
        已預先接入 <strong>150+ 技能</strong> 與 <strong>20+ 整合</strong> —
        從 GitHub、Gmail 到 Slack、Notion、Drive、Linear，以及你團隊日常使用的長尾工具。
      </p>
    </div>
    <Marquee speed={45} class="skills-marquee">
      {skills.map((s) => (
        <span class="skill-chip">
          <span class="skill-dot" aria-hidden="true"></span>
          {s}
        </span>
      ))}
    </Marquee>
    <Marquee speed={55} direction="right" class="skills-marquee skills-marquee--second">
      {skills.slice().reverse().map((s) => (
        <span class="skill-chip skill-chip--ghost">
          <span class="skill-dot" aria-hidden="true"></span>
          {s}
        </span>
      ))}
    </Marquee>
  </section>

  <!-- LIFECYCLE — the loop, end to end ───────────────────────────── -->
  <section class="aw-section-flush home-section home-section--soft">
    <div class="aw-container-wide">
      <header class="aw-section-head lifecycle-head">
        <h2 class="aw-h2">從對話到排程 App，閉環只需要一次設定。</h2>
        <p class="aw-lede lifecycle-lede">對話變成表單，表單驅動 Stage，Stage 產出 Artifact，Artifact 觸發下一次執行。</p>
      </header>
      <WorkflowLifecycle loopHint="然後不斷重複 — 依 cron、手動觸發、Webhook 呼叫" />
    </div>
  </section>

  <!-- ANATOMY ──────────────────────────────────────────────────────── -->
  <section class="aw-section-flush home-section anatomy-borderless">
    <div class="aw-container-wide">
      <AnatomyFlow
        eyebrow=""
        title="三塊拼圖。一條管道。"
        lede="輸入餵給 Stage。Stage 產出 Artifact。Artifact 流向下游，或作為下一個 Stage 的輸入。"
        steps={anatomySteps}
      />
    </div>
  </section>

  <!-- FEATURES — bento grid (first tile is wide for asymmetry) ──── -->
  <section class="aw-section-flush home-section home-section--soft">
    <div class="aw-container-wide">
      <header class="aw-section-head">
        <h2 class="aw-h2">讓一個 Agent 真正沉澱下來，所需要的一切。</h2>
      </header>
      <BentoGrid items={features} />
    </div>
  </section>

  <!-- USE CASES — editorial two-column, no card chrome ───────────── -->
  <section class="aw-section-flush home-section">
    <div class="aw-container-wide">
      <header class="aw-section-head">
        <h2 class="aw-h2">為團隊真實在做的事而設計。</h2>
      </header>

      <div class="cases-editorial">
        {cases.map((c) => (
          <article class="case">
            <a href={c.slug} class="case-link">
              <h3 class="case-title">{c.title}</h3>
              <p class="case-desc">{c.desc}</p>
              <ul class="case-points" role="list">
                {c.points.map((p) => <li>{p}</li>)}
              </ul>
              <span class="case-cta">
                查看 {c.title}
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
              </span>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- TESTIMONIALS — first one featured, others quieter ──────────── -->
  <section class="aw-section-flush home-section home-section--soft testimonials-section">
    <div class="aw-container-wide">
      <header class="aw-section-head">
        <h2 class="aw-h2">已經把閉環跑通的營運者們。</h2>
      </header>

      <ul class="testimonials" role="list">
        {testimonials.map((t) => (
          <li class:list={['testimonial', t.featured && 'testimonial--featured']}>
            <svg class="testimonial__quote" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M7.17 6A5.17 5.17 0 0 0 2 11.17V18h7v-6.83H5.5c0-1.93 1.57-3.5 3.5-3.5V6h-1.83zm10 0a5.17 5.17 0 0 0-5.17 5.17V18h7v-6.83h-3.5c0-1.93 1.57-3.5 3.5-3.5V6h-1.83z"/>
            </svg>
            <p class="testimonial__quote-text">{t.quote}</p>
            <div class="testimonial__author">
              <span class="testimonial__avatar" aria-hidden="true">{t.name.charAt(0)}</span>
              <div>
                <div class="testimonial__name">{t.name}</div>
                <div class="testimonial__title">{t.title} · {t.company}</div>
              </div>
            </div>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- FAQ ──────────────────────────────────────────────────────────── -->
  <section class="aw-section-flush home-section home-section--soft">
    <div class="aw-container-wide">
      <header class="aw-section-head faq-head">
        <h2 class="aw-h2">常見問題，一次說清。</h2>
      </header>
      <ul class="faq-list" role="list">
        {faqs.map((item) => (
          <li class="faq-item">
            <h3 class="faq-q">{item.q}</h3>
            <p class="faq-a">{item.a}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- CTA — drenched brand green, no decoration ──────────────────── -->
  <section class="aw-section-flush home-section cta-section">
    <div class="aw-container-wide">
      <div class="cta-drench">
        <h2 class="cta-h2">午餐前發布你的第一個 App。</h2>
        <p class="cta-lede">打開對話。點擊 <code class="cta-code">存為 App</code>。整個流程就是這樣。</p>
        <div class="aw-actions aw-actions-center cta-actions">
          <a href="https://app.aitroop.net" class="aw-btn cta-btn-primary">打開應用</a>
          <a href="/zh-TW/contact" class="aw-btn cta-btn-ghost">聯絡我們</a>
        </div>
      </div>
    </div>
  </section>

</Layout>

<style>
  /* ══════════════════════════════════════════════════════════════════
     HERO — centered single column, restrained backdrop
     ══════════════════════════════════════════════════════════════════ */
  .hero {
    padding-top: clamp(3.5rem, 7vw, 5.5rem);
    padding-bottom: clamp(4rem, 7vw, 6rem);
  }

  .hero-copy {
    text-align: center;
    max-width: 44rem;
    margin: 0 auto;
  }

  .hero-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.3125rem 0.875rem 0.3125rem 0.4375rem;
    border-radius: 9999px;
    border: 1px solid var(--aw-color-border);
    background: var(--aw-color-bg-page);
    font-size: 0.8125rem;
    color: var(--aw-color-text-muted);
    text-decoration: none;
    margin-bottom: 1.75rem;
    transition: border-color 200ms ease, color 200ms ease;
  }
  .hero-pill:hover {
    border-color: rgba(82, 153, 97, 0.35);
    color: var(--aw-color-text-default);
  }
  .pill-tag {
    padding: 1px 7px;
    border-radius: 9999px;
    background: rgba(82, 153, 97, 0.12);
    color: var(--aw-color-primary);
    font-family: var(--aw-font-mono, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace);
    font-weight: 700;
    font-size: 0.625rem;
    letter-spacing: 0.1em;
    line-height: 1.5;
  }
  .pill-arrow {
    width: 12px;
    height: 12px;
    color: var(--aw-color-text-muted);
    transition: transform 200ms ease;
  }
  .hero-pill:hover .pill-arrow { transform: translateX(2px); }

  .hero-display {
    font-size: clamp(2.25rem, 5vw, 3.75rem);
    line-height: 1.04;
    margin: 0 auto 1.5rem;
    max-width: 22ch;
  }
  :global(.hero-pop) {
    display: inline-flex;
    flex-wrap: nowrap;
    white-space: nowrap;
  }
  .hero-lede {
    margin: 0 auto 2rem;
    max-width: 40rem;
    font-size: clamp(1.0625rem, 1.25vw, 1.1875rem);
  }
  .hero-actions {
    justify-content: center;
  }

  @keyframes hero-rise {
    from { opacity: 0; transform: translateY(8px); }
    to   { opacity: 1; transform: none; }
  }
  .hero-pill,
  .hero-display,
  .hero-lede,
  .hero-actions {
    animation: hero-rise 700ms cubic-bezier(0.25, 1, 0.5, 1) both;
  }
  .hero-pill    { animation-delay: 0ms; }
  .hero-display { animation-delay: 80ms; }
  .hero-lede    { animation-delay: 160ms; }
  .hero-actions { animation-delay: 240ms; }

  @media (prefers-reduced-motion: reduce) {
    .hero-pill,
    .hero-display,
    .hero-lede,
    .hero-actions {
      animation: none;
    }
  }

  /* ══════════════════════════════════════════════════════════════════
     SKILLS MARQUEE — replaces the hero-metric stats strip
     ══════════════════════════════════════════════════════════════════ */
  .skills-section {
    padding: 0.5rem 0 3rem;
  }
  @media (min-width: 768px) {
    .skills-section { padding: 1rem 0 4rem; }
  }
  .skills-lede {
    text-align: center;
    max-width: 56ch;
    margin: 0 auto 2rem;
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--aw-color-text-muted);
  }
  .skills-lede strong {
    color: var(--aw-color-text-heading);
    font-weight: 600;
    font-variant-numeric: tabular-nums;
  }
  .skills-marquee { padding: 0.5rem 0; }
  .skills-marquee--second {
    margin-top: 0.5rem;
    opacity: 0.85;
  }
  .skill-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.95rem;
    border: 1px solid var(--aw-color-border);
    border-radius: 9999px;
    background: var(--aw-color-bg-page);
    font-size: 0.8125rem;
    color: var(--aw-color-text-default);
    white-space: nowrap;
    transition: border-color 200ms ease, color 200ms ease;
  }
  .skill-chip:hover {
    border-color: rgba(82, 153, 97, 0.4);
    color: var(--aw-color-primary);
  }
  .skill-chip--ghost {
    background: transparent;
    color: var(--aw-color-text-muted);
  }
  .skill-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--aw-color-primary);
  }

  /* ══════════════════════════════════════════════════════════════════
     Home section rhythm — soft tinted bands replace section borders
     ══════════════════════════════════════════════════════════════════ */
  .home-section {
    padding-top: 4rem;
    padding-bottom: 4rem;
  }
  @media (min-width: 768px) {
    .home-section { padding-top: 5.5rem; padding-bottom: 5.5rem; }
  }
  .home-section--soft {
    background: rgba(82, 153, 97, 0.04);
  }

  /* Anatomy: drop hairline borders inside the component */
  :global(.anatomy-borderless .step-card) {
    border-top: none !important;
    border-bottom: none !important;
    padding-top: 0.5rem !important;
    padding-bottom: 0.5rem !important;
  }
  @media (min-width: 1024px) {
    :global(.anatomy-borderless .step-card) {
      padding-top: 0 !important;
    }
  }

  .lifecycle-head { max-width: 56rem; margin-bottom: 2.5rem; }
  .lifecycle-lede { max-width: 52ch; margin: 0; }

  /* ══════════════════════════════════════════════════════════════════
     USE CASES — editorial two-column, no card chrome
     ══════════════════════════════════════════════════════════════════ */
  .cases-editorial {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }
  @media (min-width: 768px) {
    .cases-editorial {
      grid-template-columns: repeat(2, 1fr);
      gap: 3.5rem 4rem;
    }
  }
  .case {
    border-top: 1px solid var(--aw-color-border-strong);
    padding-top: 1.5rem;
  }
  .case-link {
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
    color: inherit;
    text-decoration: none;
  }
  .case-title {
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    font-weight: 700;
    color: var(--aw-color-text-heading);
    letter-spacing: -0.025em;
    line-height: 1.05;
    margin: 0;
  }
  .case-desc {
    color: var(--aw-color-text-default);
    font-size: 1rem;
    line-height: 1.6;
    margin: 0;
    max-width: 48ch;
    text-wrap: pretty;
  }
  .case-points {
    list-style: none;
    margin: 0.25rem 0 0;
    padding: 0;
    display: grid;
    gap: 0.375rem;
    color: var(--aw-color-text-muted);
    font-size: 0.9375rem;
    line-height: 1.55;
  }
  .case-points li::before {
    content: '— ';
    color: var(--aw-color-primary);
    margin-right: 0.125rem;
  }
  .case-cta {
    margin-top: 0.5rem;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--aw-color-primary);
    font-weight: 600;
    font-size: 0.9375rem;
    transition: gap 200ms cubic-bezier(0.25, 1, 0.5, 1);
  }
  .case-cta svg {
    width: 16px;
    height: 16px;
    transition: transform 200ms cubic-bezier(0.25, 1, 0.5, 1);
  }
  .case-link:hover .case-cta {
    gap: 0.625rem;
  }
  .case-link:hover .case-cta svg {
    transform: translateX(2px);
  }

  /* ══════════════════════════════════════════════════════════════════
     TESTIMONIALS — asymmetric weight, no card chrome
     ══════════════════════════════════════════════════════════════════ */
  .testimonials-section { position: relative; }
  .testimonials {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }
  @media (min-width: 900px) {
    .testimonials {
      grid-template-columns: 1.4fr 1fr 1fr;
      gap: 2rem 2.5rem;
      align-items: start;
    }
  }
  .testimonial {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
  .testimonial__quote {
    width: 22px;
    height: 22px;
    color: rgba(82, 153, 97, 0.55);
    flex-shrink: 0;
  }
  .testimonial__quote-text {
    margin: 0;
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--aw-color-text-default);
    text-wrap: pretty;
  }
  .testimonial--featured .testimonial__quote { width: 30px; height: 30px; }
  .testimonial--featured .testimonial__quote-text {
    font-size: clamp(1.0625rem, 1.4vw, 1.25rem);
    line-height: 1.55;
    color: var(--aw-color-text-heading);
    font-weight: 500;
  }
  .testimonial__author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: auto;
  }
  .testimonial__avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: rgba(82, 153, 97, 0.12);
    color: var(--aw-color-primary);
    font-weight: 700;
    font-size: 0.875rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .testimonial__name {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--aw-color-text-heading);
    line-height: 1.2;
  }
  .testimonial__title {
    font-size: 0.75rem;
    color: var(--aw-color-text-muted);
    letter-spacing: 0.01em;
    line-height: 1.4;
    margin-top: 2px;
  }

  /* ══════════════════════════════════════════════════════════════════
     FAQ — borderless, two columns on desktop
     ══════════════════════════════════════════════════════════════════ */
  .faq-head { max-width: 56rem; }
  .faq-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.75rem 2.5rem;
  }
  @media (min-width: 768px) {
    .faq-list { grid-template-columns: repeat(2, 1fr); }
  }
  .faq-item { max-width: 44ch; }
  .faq-q {
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--aw-color-text-heading);
    margin: 0 0 0.5rem;
    line-height: 1.35;
  }
  .faq-a {
    margin: 0;
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--aw-color-text-muted);
  }

  /* ══════════════════════════════════════════════════════════════════
     CTA — drenched brand green
     ══════════════════════════════════════════════════════════════════ */
  .cta-section {
    padding-top: 3rem;
    padding-bottom: 5rem;
  }
  .cta-drench {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    border-radius: 20px;
    background: var(--aw-color-primary);
    color: #fff;
    padding: 3.25rem 1.75rem 3.5rem;
    text-align: center;
  }
  @media (min-width: 768px) {
    .cta-drench { padding: 4.5rem 2.5rem 4.75rem; }
  }
  /* subtle inner light — a single thin highlight, no aurora/meteors */
  .cta-drench::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 70% 60% at 50% 0%, rgba(255, 255, 255, 0.18), transparent 65%);
    pointer-events: none;
    z-index: 0;
  }
  .cta-drench > * { position: relative; z-index: 1; }
  .cta-h2 {
    font-size: clamp(2rem, 4vw, 2.875rem);
    font-weight: 700;
    letter-spacing: -0.025em;
    line-height: 1.08;
    color: #fff;
    margin: 0 auto 1rem;
    max-width: 20ch;
    text-wrap: balance;
  }
  .cta-lede {
    margin: 0 auto 2rem;
    max-width: 46ch;
    color: rgba(255, 255, 255, 0.86);
    font-size: 1.0625rem;
    line-height: 1.6;
  }
  .cta-code {
    font-family: var(--aw-font-mono, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace);
    font-size: 0.86em;
    padding: 0.12em 0.5em;
    border-radius: 5px;
    background: rgba(255, 255, 255, 0.16);
    border: 1px solid rgba(255, 255, 255, 0.22);
    color: #fff;
    font-weight: 500;
  }
  .cta-actions {
    gap: 0.75rem;
  }
  .cta-btn-primary {
    background: #fff;
    color: var(--aw-color-accent);
    border-color: rgba(0, 0, 0, 0.04);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.6),
      0 1px 2px rgba(20, 50, 30, 0.18),
      0 8px 18px -8px rgba(20, 50, 30, 0.45);
  }
  .cta-btn-primary:hover {
    background: #fafafa;
    color: #1c4d2b;
  }
  .cta-btn-ghost {
    background: transparent;
    color: #fff;
    border-color: rgba(255, 255, 255, 0.42);
  }
  .cta-btn-ghost:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.7);
    color: #fff;
  }
  .cta-btn-primary:focus-visible,
  .cta-btn-ghost:focus-visible {
    outline: none;
    box-shadow:
      0 0 0 2px var(--aw-color-primary),
      0 0 0 4px #fff;
  }
</style>
