---
import Layout from '~/layouts/PageLayout.astro';
import AnatomyFlow from '~/components/widgets/AnatomyFlow.astro';
import WorkflowLifecycle from '~/components/widgets/WorkflowLifecycle.astro';
import PopText from '~/components/ui/PopText.astro';
import Spotlight from '~/components/ui/Spotlight.astro';
import GridBackground from '~/components/ui/GridBackground.astro';
import BentoGrid from '~/components/ui/BentoGrid.astro';
import HoverGlow from '~/components/ui/HoverGlow.astro';
import HoverBorderGradient from '~/components/ui/HoverBorderGradient.astro';
import GlareHover from '~/components/ui/GlareHover.astro';
import Aurora from '~/components/ui/Aurora.astro';
import Sparkles from '~/components/ui/Sparkles.astro';
import Meteors from '~/components/ui/Meteors.astro';
import Marquee from '~/components/ui/Marquee.astro';
import Tilt3D from '~/components/ui/Tilt3D.astro';
import AnimatedCounter from '~/components/ui/AnimatedCounter.astro';

const metadata = {
  title: 'Aitroop - 把 AI Agent 对话固化成 App',
  ignoreTitleTemplate: true,
};

const anatomySteps = [
  {
    title: 'AppInput',
    desc: '基于 Schema 的表单。text、select、daterange、file，声明一次，自动渲染。',
    foot: '4 种输入类型 · 内置校验',
  },
  {
    title: 'Stage',
    desc: '带明确目标的 Agent 会话，连入你的技能与集成。需要时多 Stage 串联成管道。',
    foot: '150+ 技能 · 20+ 集成',
  },
  {
    title: 'Artifact',
    desc: '可交付的产物。预览、导出，或直接喂给下一个 Stage。',
    foot: '7 种格式 · 预览 + 导出',
  },
];

const stats = [
  { value: 150, suffix: '+', label: '技能' },
  { value: 20,  suffix: '+', label: '集成' },
  { value: 7,   suffix: '',  label: '产物格式' },
  { value: 'cron', suffix: '', label: '调度' },
];

const skills = [
  'GitHub', 'Gmail', 'Slack', 'Notion', 'Drive', 'Linear',
  'Stripe', 'HubSpot', 'Salesforce', 'Figma', 'Jira', 'Asana',
  'Webhook', '网页搜索', '代码执行', 'PDF', 'CSV', 'Postgres',
  'Discord', 'Twitter', 'YouTube', '日历', 'Sheets', 'Airtable',
];

const testimonials = [
  {
    quote: '我们用 3 个 App 顶替了一个 4 人运营小组的日常循环。它们按 cron 跑，没人需要再去碰它们。',
    name: 'Marta R.',
    title: '营收运营主管',
    company: 'Northbound',
  },
  {
    quote: '「存为 App」让我那堆混乱的 prompt 笔记，变成了团队真的能跑起来的东西。',
    name: 'Diego K.',
    title: '解决方案架构师',
    company: 'Plate',
  },
  {
    quote: '沙箱才是杀手锏。真实的文件访问、真实的工具、真实的产物——不是玩具 demo。',
    name: 'Priya S.',
    title: 'AI 总监',
    company: 'Vector Labs',
  },
];

const features = [
  {
    title: '对话存为 App',
    desc: '一键沉淀刚跑通的流程，变成带类型化输入、可重复运行的工作流。',
    icon: 'M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z',
  },
  {
    title: '多 Stage 管道',
    desc: '单次 Agent 解决不了的任务，就用 Stage 串联起来。每个 Stage 拥有自己的模型、技能与 prompt。',
    icon: 'M4 6h16M4 12h16M4 18h10',
  },
  {
    title: '150+ 技能 · 20+ 集成',
    desc: 'GitHub、Gmail、Slack、Drive、Notion、网页搜索、代码执行，一键启用。',
    icon: 'M3 3h7v7H3zM14 3h7v7h-7zM3 14h7v7H3zM14 14h7v7h-7z',
  },
  {
    title: '真实沙箱',
    desc: '每一次运行都落在隔离环境里，提供文件系统、网络和合适的工具链。',
    icon: 'M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z',
  },
  {
    title: '调度与 Webhook',
    desc: '按 cron 定时、手动触发，或由 Webhook 触发。配置一次，长期托管，运行历史可追溯。',
    icon: 'M12 6v6l4 2M12 22a10 10 0 1 1 0-20 10 10 0 0 1 0 20z',
  },
  {
    title: '团队共享',
    desc: '每个 App 都是带权限的对象。可以分享给同事、外部协作者，或作为内部 API 暴露。',
    icon: 'M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2M9 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8zM23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75',
  },
];

const cases = [
  {
    slug: '/zh-CN/use-cases/gtm',
    title: 'GTM',
    desc: '线索挖掘、外部触达、客户留存、管道洞察。',
    points: ['规模化的 ICP 调研', '个性化外联序列', '管道与留存报告'],
  },
  {
    slug: '/zh-CN/use-cases/finance',
    title: '财务',
    desc: '月结、稽核、差异分析、报告。专为 CFO 团队设计。',
    points: ['月结报告包自动装配', '差异与变动分析', '审计就绪的导出'],
  },
];

const faqs = [
  {
    q: '把对话存成 App，要不要重写 prompt？',
    a: '不用。在已跑通的对话里点一下「存为 App」，Aitroop 会把当时使用的技能、集成与 prompt 都打包成可运行的工作流。',
  },
  {
    q: 'App 保存好之后，谁来执行？',
    a: '每次运行都跑在隔离沙箱里，自带文件系统、网络和 Agent 需要的工具。可以手动触发、按 cron 定时，或者从 Webhook 触发。',
  },
  {
    q: '步骤需要用不同的工具，能串起来吗？',
    a: '可以。Stage 让你把一个 Agent 接到下一个 Agent，每个 Stage 拥有独立的模型、技能与 prompt。前一个 Stage 的产物自动成为下一个的输入。',
  },
  {
    q: 'App 发布后谁可以使用？',
    a: 'App 是带权限的对象。可以分享给同事、外部协作者，或作为内部 API 暴露。运行历史对管理员可见。',
  },
];
---

<Layout metadata={metadata}>
  <Spotlight />
  <GlareHover />

  <!-- HERO — grid + aurora + sparkles ─────────────────────────────── -->
  <section class="aw-hero hero relative isolate overflow-hidden">
    <GridBackground variant="grid" tone="brand" />
    <Aurora intensity="subtle" position="top" />
    <Sparkles count={42} size={2} />
    <div class="aw-container-wide">
      <div class="hero-grid">
        <div class="hero-copy">
          <a href="/zh-CN/use-cases" class="hero-pill">
            <span class="pill-tag">NEW</span>
            <span class="pill-text">150+ 技能 · 20+ 集成</span>
            <svg class="pill-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
          </a>
          <h1 class="aw-display hero-display">
            强大的 Agent。<br />
            无尽的技能。<br />
            <PopText
              text="一键即用的 App。"
              stagger={32}
              delay={260}
              duration={310}
              class="text-primary hero-pop"
            />
          </h1>
          <p class="aw-lede hero-lede">
            一键就把刚跑通的 Agent 流程沉淀为 App，团队可以反复运行。不再重敲 prompt，不再写胶水代码。
          </p>
          <div class="aw-actions hero-actions">
            <a href="https://app.aitroop.net" class="aw-btn aw-btn-primary">免费开始</a>
            <a href="/zh-CN/use-cases" class="aw-btn aw-btn-ghost">查看案例</a>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- STATS — animated counters strip ─────────────────────────────── -->
  <section class="stats-section">
    <div class="aw-container-wide">
      <ul class="stats" role="list">
        {stats.map((s) => (
          <li class="stat">
            <span class="stat-value">
              <AnimatedCounter value={s.value} suffix={s.suffix} />
            </span>
            <span class="stat-label">{s.label}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- SKILLS MARQUEE — infinite scroller of connect chips ─────────── -->
  <section class="skills-section">
    <div class="aw-container-wide">
      <p class="skills-eyebrow">适配你团队已经在用的工具</p>
    </div>
    <Marquee speed={45} class="skills-marquee">
      {skills.map((s) => (
        <span class="skill-chip">
          <span class="skill-dot" aria-hidden="true"></span>
          {s}
        </span>
      ))}
    </Marquee>
    <Marquee speed={55} direction="right" class="skills-marquee skills-marquee--second">
      {skills.slice().reverse().map((s) => (
        <span class="skill-chip skill-chip--ghost">
          <span class="skill-dot" aria-hidden="true"></span>
          {s}
        </span>
      ))}
    </Marquee>
  </section>

  <!-- LIFECYCLE — the loop, end to end ───────────────────────────── -->
  <section class="aw-section-flush home-section home-section--soft">
    <div class="aw-container-wide">
      <header class="aw-section-head lifecycle-head">
        <h2 class="aw-h2">从对话到定时 App，闭环只用一次配置。</h2>
        <p class="aw-lede lifecycle-lede">对话变成表单，表单驱动 Stage，Stage 产出 Artifact，Artifact 触发下一次运行。</p>
      </header>
      <WorkflowLifecycle loopHint="然后不断重复 — 按 cron、手动触发、Webhook 调用" />
    </div>
  </section>

  <!-- ANATOMY ──────────────────────────────────────────────────────── -->
  <section class="aw-section-flush home-section anatomy-borderless">
    <div class="aw-container-wide">
      <AnatomyFlow
        eyebrow=""
        title="三块拼图。一条管道。"
        lede="输入喂给 Stage。Stage 产出 Artifact。Artifact 流向下游，或作为下一个 Stage 的输入。"
        steps={anatomySteps}
      />
    </div>
  </section>

  <!-- FEATURES — bento grid ──────────────────────────────────────── -->
  <section class="aw-section-flush home-section home-section--soft">
    <div class="aw-container-wide">
      <header class="aw-section-head">
        <p class="aw-eyebrow">团队为什么选择 Aitroop</p>
        <h2 class="aw-h2">让一个 Agent 真正沉淀下来，所需的一切。</h2>
      </header>
      <BentoGrid items={features} />
    </div>
  </section>

  <!-- USE CASES — 3D tilt cards with cipher hover ─────────────────── -->
  <HoverGlow />
  <Tilt3D />
  <section class="aw-section-flush home-section">
    <div class="aw-container-wide">
      <header class="aw-section-head">
        <p class="aw-eyebrow">案例</p>
        <h2 class="aw-h2">为团队真实在做的事而设计。</h2>
      </header>

      <ul class="cases-grid" role="list">
        {cases.map((c) => (
          <li class="case-card tilt-3d hover-glow glare-hover" data-tilt-3d data-hover-glow>
            <div class="hover-glow__cipher" aria-hidden="true"></div>
            <div class="hover-glow__mask" aria-hidden="true"></div>
            <a href={c.slug} class="case-link">
              <div class="case-head">
                <span class="case-title" data-tilt-pop>{c.title}</span>
                <span class="case-arrow aw-arrow" aria-hidden="true">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
                </span>
              </div>
              <p class="case-desc">{c.desc}</p>
              <ul class="case-points" role="list">
                {c.points.map((p) => (
                  <li>
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
                    <span>{p}</span>
                  </li>
                ))}
              </ul>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- TESTIMONIALS — quote cards with glare + spotlight ───────────── -->
  <section class="aw-section-flush home-section home-section--soft testimonials-section">
    <div class="aw-container-wide">
      <header class="aw-section-head">
        <p class="aw-eyebrow">深得 AI 优先团队信赖</p>
        <h2 class="aw-h2">已经把闭环跑通的运营者们。</h2>
      </header>

      <ul class="testimonials" role="list">
        {testimonials.map((t) => (
          <li class="testimonial spotlight-card glare-hover" data-spotlight>
            <svg class="testimonial__quote" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M7.17 6A5.17 5.17 0 0 0 2 11.17V18h7v-6.83H5.5c0-1.93 1.57-3.5 3.5-3.5V6h-1.83zm10 0a5.17 5.17 0 0 0-5.17 5.17V18h7v-6.83h-3.5c0-1.93 1.57-3.5 3.5-3.5V6h-1.83z"/>
            </svg>
            <p class="testimonial__quote-text">{t.quote}</p>
            <div class="testimonial__author">
              <span class="testimonial__avatar" aria-hidden="true">{t.name.charAt(0)}</span>
              <div>
                <div class="testimonial__name">{t.name}</div>
                <div class="testimonial__title">{t.title} · {t.company}</div>
              </div>
            </div>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- FAQ ──────────────────────────────────────────────────────────── -->
  <section class="aw-section-flush home-section home-section--soft">
    <div class="aw-container-wide">
      <header class="aw-section-head faq-head">
        <p class="aw-eyebrow">FAQ</p>
        <h2 class="aw-h2">常见问题，一次说清。</h2>
      </header>
      <ul class="faq-list" role="list">
        {faqs.map((item) => (
          <li class="faq-item">
            <h3 class="faq-q">{item.q}</h3>
            <p class="faq-a">{item.a}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- CTA — conic border + meteors + aurora ──────────────────────── -->
  <section class="aw-section-flush home-section cta-section">
    <div class="aw-container-wide">
      <HoverBorderGradient radius="20px" thickness={1} class="cta-hbg">
        <div class="cta-card">
          <Aurora intensity="bold" position="center" class="cta-aurora" />
          <Meteors count={14} angle={215} class="cta-meteors" />
          <div class="cta-content">
            <h2 class="aw-h2 cta-h2">午饭前发布你的第一个 App。</h2>
            <p class="aw-lede cta-lede">打开对话。点击 <code class="aw-code">存为 App</code>。整个流程就是这样。</p>
            <div class="aw-actions aw-actions-center">
              <a href="https://app.aitroop.net" class="aw-btn aw-btn-primary">打开应用</a>
              <a href="/zh-CN/contact" class="aw-btn aw-btn-ghost">联系我们</a>
            </div>
          </div>
        </div>
      </HoverBorderGradient>
    </div>
  </section>

</Layout>

<style>
  /* ══════════════════════════════════════════════════════════════════
     HERO — split layout
     ══════════════════════════════════════════════════════════════════ */
  .hero {
    padding-top: clamp(3rem, 7vw, 5rem);
    padding-bottom: clamp(3rem, 6vw, 5rem);
  }

  .hero-grid {
    display: block;
  }

  .hero-copy {
    text-align: center;
    max-width: 44rem;
    margin: 0 auto;
  }

  .hero-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.3125rem 0.875rem 0.3125rem 0.4375rem;
    border-radius: 9999px;
    border: 1px solid var(--aw-color-border);
    background: var(--aw-color-bg-page);
    font-size: 0.8125rem;
    color: var(--aw-color-text-muted);
    text-decoration: none;
    margin-bottom: 1.75rem;
    transition: border-color 200ms ease, color 200ms ease, transform 200ms ease;
  }
  .hero-pill:hover {
    border-color: rgba(82, 153, 97, 0.35);
    color: var(--aw-color-text-default);
  }
  .pill-tag {
    padding: 1px 7px;
    border-radius: 9999px;
    background: rgba(82, 153, 97, 0.12);
    color: var(--aw-color-primary);
    font-family: var(--aw-font-mono, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace);
    font-weight: 700;
    font-size: 0.625rem;
    letter-spacing: 0.1em;
    line-height: 1.5;
  }
  .pill-arrow {
    width: 12px;
    height: 12px;
    color: var(--aw-color-text-muted);
    transition: transform 200ms ease;
  }
  .hero-pill:hover .pill-arrow { transform: translateX(2px); }

  .hero-display {
    font-size: clamp(2.5rem, 5vw, 3.75rem);
    line-height: 1.04;
    margin: 0 auto 1.5rem;
    max-width: 22ch;
  }
  :global(.hero-pop) {
    display: inline-flex;
    flex-wrap: nowrap;
    white-space: nowrap;
  }
  .hero-lede {
    margin: 0 auto 2rem;
    max-width: 40rem;
    font-size: clamp(1.0625rem, 1.25vw, 1.1875rem);
  }
  .hero-actions {
    justify-content: center;
  }

  @keyframes hero-rise {
    from { opacity: 0; transform: translateY(8px); }
    to   { opacity: 1; transform: none; }
  }
  .hero-pill,
  .hero-display,
  .hero-lede,
  .hero-actions {
    animation: hero-rise 700ms cubic-bezier(0.2, 0.7, 0.2, 1) both;
  }
  .hero-pill    { animation-delay: 0ms; }
  .hero-display { animation-delay: 80ms; }
  .hero-lede    { animation-delay: 160ms; }
  .hero-actions { animation-delay: 240ms; }

  @media (prefers-reduced-motion: reduce) {
    .hero-pill,
    .hero-display,
    .hero-lede,
    .hero-actions {
      animation: none;
    }
  }

  /* ── Stats ─ minimal strip, no borders, lots of air ──────────── */
  .stats-section {
    padding: 2rem 0 3rem;
  }
  @media (min-width: 768px) {
    .stats-section { padding: 2.75rem 0 3.5rem; }
  }
  .stats {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.75rem 1rem;
  }
  @media (min-width: 768px) {
    .stats {
      grid-template-columns: repeat(4, 1fr);
      gap: 1rem;
    }
  }
  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .stat-value {
    font-family: var(--aw-font-mono, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace);
    font-size: 1.625rem;
    font-weight: 500;
    color: var(--aw-color-text-heading);
    letter-spacing: -0.02em;
    line-height: 1;
    font-variant-numeric: tabular-nums;
  }
  .stat-label {
    font-size: 0.75rem;
    color: var(--aw-color-text-muted);
    letter-spacing: 0.04em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  /* ── Home sections — divider-free rhythm via soft tints ──────── */
  .home-section {
    padding-top: 4rem;
    padding-bottom: 4rem;
  }
  @media (min-width: 768px) {
    .home-section { padding-top: 5.5rem; padding-bottom: 5.5rem; }
  }
  .home-section--soft {
    background: rgba(82, 153, 97, 0.025);
  }

  /* Anatomy: remove the hairline borders inside the component */
  :global(.anatomy-borderless .step-card) {
    border-top: none !important;
    border-bottom: none !important;
    padding-top: 0.5rem !important;
    padding-bottom: 0.5rem !important;
  }
  @media (min-width: 1024px) {
    :global(.anatomy-borderless .step-card) {
      padding-top: 0 !important;
    }
  }

  /* Lifecycle section heading uses a tighter rhythm */
  .lifecycle-head { max-width: 56rem; margin-bottom: 2.5rem; }
  .lifecycle-lede { max-width: 52ch; margin: 0; }

  /* ── Use-case cards (no row borders) ─────────────────────────── */
  .cases-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }
  @media (min-width: 768px) {
    .cases-grid { grid-template-columns: repeat(2, 1fr); gap: 1.5rem; }
  }
  .case-card {
    border-radius: 16px;
    background: var(--aw-color-bg-page);
    border: 1px solid var(--aw-color-border);
    transition: border-color 200ms ease, transform 200ms ease, box-shadow 200ms ease;
  }
  .case-card:hover {
    border-color: rgba(82, 153, 97, 0.35);
    transform: translateY(-2px);
    box-shadow: 0 18px 40px -22px rgba(10, 10, 10, 0.18);
  }
  .case-link {
    display: block;
    padding: 1.75rem 1.75rem 1.875rem;
    color: inherit;
    text-decoration: none;
  }
  .case-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.625rem;
  }
  .case-title {
    font-size: 1.625rem;
    font-weight: 700;
    color: var(--aw-color-text-heading);
    letter-spacing: -0.02em;
    line-height: 1.1;
  }
  .case-desc {
    color: var(--aw-color-text-muted);
    font-size: 0.9375rem;
    line-height: 1.55;
    margin: 0 0 1.125rem;
  }
  .case-arrow {
    color: var(--aw-color-text-muted);
    transition: transform 200ms ease, color 200ms ease;
  }
  .case-card:hover .case-arrow {
    transform: translateX(3px);
    color: var(--aw-color-primary);
  }
  .case-points {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.5rem;
  }
  .case-points li {
    display: flex;
    align-items: flex-start;
    gap: 0.625rem;
    font-size: 0.9rem;
    color: var(--aw-color-text-default);
    line-height: 1.5;
  }
  .case-points svg {
    width: 16px;
    height: 16px;
    color: var(--aw-color-primary);
    margin-top: 2px;
    flex-shrink: 0;
  }

  /* ── FAQ — borderless, two columns on desktop ────────────────── */
  .faq-head { max-width: 56rem; }
  .faq-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.75rem 2.5rem;
  }
  @media (min-width: 768px) {
    .faq-list { grid-template-columns: repeat(2, 1fr); }
  }
  .faq-item { max-width: 44ch; }
  .faq-q {
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--aw-color-text-heading);
    margin: 0 0 0.5rem;
    line-height: 1.35;
  }
  .faq-a {
    margin: 0;
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--aw-color-text-muted);
  }

  /* ── CTA card ────────────────────────────────────────────────── */
  .cta-section {
    padding-top: 3rem;
    padding-bottom: 5rem;
  }
  .cta-card {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    border-radius: inherit;
    background:
      radial-gradient(ellipse 60% 80% at 50% 0%, rgba(82, 153, 97, 0.10), transparent 70%),
      var(--aw-color-bg-page);
    padding: 3rem 1.5rem 3.25rem;
    text-align: center;
  }
  @media (min-width: 768px) {
    .cta-card { padding: 4rem 2rem 4.25rem; }
  }
  :global(.cta-card .cta-aurora)  { z-index: 0; opacity: 0.6; }
  :global(.cta-card .cta-meteors) { z-index: 0; opacity: 0.85; }
  .cta-content { position: relative; z-index: 2; }
  .cta-h2 {
    font-size: clamp(2rem, 4vw, 2.75rem);
    margin: 0 auto 1rem;
    max-width: 18ch;
  }
  .cta-lede {
    margin: 0 auto 1.75rem;
    max-width: 44ch;
  }

  /* ══════════════════════════════════════════════════════════════════
     SKILLS MARQUEE
     ══════════════════════════════════════════════════════════════════ */
  .skills-section {
    padding: 1.5rem 0 3rem;
  }
  @media (min-width: 768px) {
    .skills-section { padding: 1.75rem 0 3.5rem; }
  }
  .skills-eyebrow {
    font-family: var(--aw-font-mono, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace);
    font-size: 0.7rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--aw-color-text-muted);
    text-align: center;
    margin: 0 0 1.5rem;
  }
  .skills-marquee {
    padding: 0.5rem 0;
  }
  .skills-marquee--second {
    margin-top: 0.5rem;
    opacity: 0.85;
  }
  .skill-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.95rem;
    border: 1px solid var(--aw-color-border);
    border-radius: 9999px;
    background: var(--aw-color-bg-page);
    font-size: 0.8125rem;
    color: var(--aw-color-text-default);
    white-space: nowrap;
    transition: border-color 200ms ease, color 200ms ease, transform 200ms ease;
  }
  .skill-chip:hover {
    border-color: rgba(82, 153, 97, 0.4);
    color: var(--aw-color-primary);
    transform: translateY(-1px);
  }
  .skill-chip--ghost {
    background: transparent;
    color: var(--aw-color-text-muted);
  }
  .skill-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--aw-color-primary);
    box-shadow: 0 0 8px rgba(82, 153, 97, 0.55);
  }

  /* ══════════════════════════════════════════════════════════════════
     TESTIMONIALS
     ══════════════════════════════════════════════════════════════════ */
  .testimonials-section { position: relative; }
  .testimonials {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  @media (min-width: 768px) {
    .testimonials { grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }
  }
  .testimonial {
    position: relative;
    overflow: hidden;
    padding: 1.75rem 1.5rem 1.5rem;
    border: 1px solid var(--aw-color-border);
    border-radius: 16px;
    background: var(--aw-color-bg-page);
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    transition: border-color 220ms ease, transform 240ms ease, box-shadow 240ms ease;
  }
  .testimonial:hover {
    border-color: rgba(82, 153, 97, 0.32);
    transform: translateY(-2px);
    box-shadow: 0 22px 50px -28px rgba(10, 10, 10, 0.18);
  }
  .testimonial__quote {
    width: 22px;
    height: 22px;
    color: rgba(82, 153, 97, 0.35);
    flex-shrink: 0;
  }
  .testimonial__quote-text {
    margin: 0;
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--aw-color-text-default);
    text-wrap: pretty;
  }
  .testimonial__author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: auto;
    padding-top: 0.75rem;
    border-top: 1px solid var(--aw-color-border);
  }
  .testimonial__avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(82, 153, 97, 0.2), rgba(82, 153, 97, 0.05));
    color: var(--aw-color-primary);
    font-weight: 700;
    font-size: 0.875rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    border: 1px solid rgba(82, 153, 97, 0.25);
  }
  .testimonial__name {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--aw-color-text-heading);
    line-height: 1.2;
  }
  .testimonial__title {
    font-size: 0.75rem;
    color: var(--aw-color-text-muted);
    letter-spacing: 0.01em;
    line-height: 1.4;
    margin-top: 2px;
  }
</style>
